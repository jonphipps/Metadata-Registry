# default values
all:
  title_prefix:        "The Registry! :: "
  base_domain:         "http://registry/uri/"
  default_language:    en
  default_status_id:   1
  default_conneg_type: rdf
  schema_profile_id:   1
  admin_email:         sysadmin@metadataregistry.org
