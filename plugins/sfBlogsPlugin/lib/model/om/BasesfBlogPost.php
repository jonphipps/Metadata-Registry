<?php

/**
 * Base class that represents a row from the 'sf_blog_post' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Wed Apr 22 19:56:25 2009
 *
 * @package    plugins.sfBlogsPlugin.lib.model.om
 */
abstract class BasesfBlogPost extends BaseObject  implements Persistent {


  const PEER = 'sfBlogPostPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        sfBlogPostPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the sf_blog_id field.
	 * @var        int
	 */
	protected $sf_blog_id;

	/**
	 * The value for the author_id field.
	 * @var        int
	 */
	protected $author_id;

	/**
	 * The value for the title field.
	 * @var        string
	 */
	protected $title;

	/**
	 * The value for the stripped_title field.
	 * @var        string
	 */
	protected $stripped_title;

	/**
	 * The value for the extract field.
	 * @var        string
	 */
	protected $extract;

	/**
	 * The value for the content field.
	 * @var        string
	 */
	protected $content;

	/**
	 * The value for the is_published field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_published;

	/**
	 * The value for the allow_comments field.
	 * Note: this column has a database default value of: true
	 * @var        boolean
	 */
	protected $allow_comments;

	/**
	 * The value for the nb_comments field.
	 * Note: this column has a database default value of: 0
	 * @var        int
	 */
	protected $nb_comments;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * The value for the published_at field.
	 * @var        string
	 */
	protected $published_at;

	/**
	 * @var        sfBlog
	 */
	protected $asfBlog;

	/**
	 * @var        sfGuardUser
	 */
	protected $asfGuardUser;

	/**
	 * @var        array sfBlogComment[] Collection to store aggregation of sfBlogComment objects.
	 */
	protected $collsfBlogComments;

	/**
	 * @var        Criteria The criteria used to select the current contents of collsfBlogComments.
	 */
	private $lastsfBlogCommentCriteria = null;

	/**
	 * @var        array sfBlogTag[] Collection to store aggregation of sfBlogTag objects.
	 */
	protected $collsfBlogTags;

	/**
	 * @var        Criteria The criteria used to select the current contents of collsfBlogTags.
	 */
	private $lastsfBlogTagCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BasesfBlogPost object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->is_published = false;
		$this->allow_comments = true;
		$this->nb_comments = 0;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [sf_blog_id] column value.
	 * 
	 * @return     int
	 */
	public function getSfBlogId()
	{
		return $this->sf_blog_id;
	}

	/**
	 * Get the [author_id] column value.
	 * 
	 * @return     int
	 */
	public function getAuthorId()
	{
		return $this->author_id;
	}

	/**
	 * Get the [title] column value.
	 * 
	 * @return     string
	 */
	public function getTitle()
	{
		return $this->title;
	}

	/**
	 * Get the [stripped_title] column value.
	 * 
	 * @return     string
	 */
	public function getStrippedTitle()
	{
		return $this->stripped_title;
	}

	/**
	 * Get the [extract] column value.
	 * 
	 * @return     string
	 */
	public function getExtract()
	{
		return $this->extract;
	}

	/**
	 * Get the [content] column value.
	 * 
	 * @return     string
	 */
	public function getContent()
	{
		return $this->content;
	}

	/**
	 * Get the [is_published] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsPublished()
	{
		return $this->is_published;
	}

	/**
	 * Get the [allow_comments] column value.
	 * 
	 * @return     boolean
	 */
	public function getAllowComments()
	{
		return $this->allow_comments;
	}

	/**
	 * Get the [nb_comments] column value.
	 * 
	 * @return     int
	 */
	public function getNbComments()
	{
		return $this->nb_comments;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}


		if ($this->updated_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->updated_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [published_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getPublishedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->published_at === null) {
			return null;
		}


		if ($this->published_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->published_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->published_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = sfBlogPostPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [sf_blog_id] column.
	 * 
	 * @param      int $v new value
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setSfBlogId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->sf_blog_id !== $v) {
			$this->sf_blog_id = $v;
			$this->modifiedColumns[] = sfBlogPostPeer::SF_BLOG_ID;
		}

		if ($this->asfBlog !== null && $this->asfBlog->getId() !== $v) {
			$this->asfBlog = null;
		}

		return $this;
	} // setSfBlogId()

	/**
	 * Set the value of [author_id] column.
	 * 
	 * @param      int $v new value
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setAuthorId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->author_id !== $v) {
			$this->author_id = $v;
			$this->modifiedColumns[] = sfBlogPostPeer::AUTHOR_ID;
		}

		if ($this->asfGuardUser !== null && $this->asfGuardUser->getId() !== $v) {
			$this->asfGuardUser = null;
		}

		return $this;
	} // setAuthorId()

	/**
	 * Set the value of [title] column.
	 * 
	 * @param      string $v new value
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setTitle($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->title !== $v) {
			$this->title = $v;
			$this->modifiedColumns[] = sfBlogPostPeer::TITLE;
		}

		return $this;
	} // setTitle()

	/**
	 * Set the value of [stripped_title] column.
	 * 
	 * @param      string $v new value
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setStrippedTitle($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->stripped_title !== $v) {
			$this->stripped_title = $v;
			$this->modifiedColumns[] = sfBlogPostPeer::STRIPPED_TITLE;
		}

		return $this;
	} // setStrippedTitle()

	/**
	 * Set the value of [extract] column.
	 * 
	 * @param      string $v new value
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setExtract($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->extract !== $v) {
			$this->extract = $v;
			$this->modifiedColumns[] = sfBlogPostPeer::EXTRACT;
		}

		return $this;
	} // setExtract()

	/**
	 * Set the value of [content] column.
	 * 
	 * @param      string $v new value
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setContent($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->content !== $v) {
			$this->content = $v;
			$this->modifiedColumns[] = sfBlogPostPeer::CONTENT;
		}

		return $this;
	} // setContent()

	/**
	 * Set the value of [is_published] column.
	 * 
	 * @param      boolean $v new value
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setIsPublished($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_published !== $v || $v === false) {
			$this->is_published = $v;
			$this->modifiedColumns[] = sfBlogPostPeer::IS_PUBLISHED;
		}

		return $this;
	} // setIsPublished()

	/**
	 * Set the value of [allow_comments] column.
	 * 
	 * @param      boolean $v new value
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setAllowComments($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->allow_comments !== $v || $v === true) {
			$this->allow_comments = $v;
			$this->modifiedColumns[] = sfBlogPostPeer::ALLOW_COMMENTS;
		}

		return $this;
	} // setAllowComments()

	/**
	 * Set the value of [nb_comments] column.
	 * 
	 * @param      int $v new value
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setNbComments($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->nb_comments !== $v || $v === 0) {
			$this->nb_comments = $v;
			$this->modifiedColumns[] = sfBlogPostPeer::NB_COMMENTS;
		}

		return $this;
	} // setNbComments()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = sfBlogPostPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = sfBlogPostPeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Sets the value of [published_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     sfBlogPost The current object (for fluent API support)
	 */
	public function setPublishedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->published_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->published_at !== null && $tmpDt = new DateTime($this->published_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->published_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = sfBlogPostPeer::PUBLISHED_AT;
			}
		} // if either are not null

		return $this;
	} // setPublishedAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(sfBlogPostPeer::IS_PUBLISHED,sfBlogPostPeer::ALLOW_COMMENTS,sfBlogPostPeer::NB_COMMENTS))) {
				return false;
			}

			if ($this->is_published !== false) {
				return false;
			}

			if ($this->allow_comments !== true) {
				return false;
			}

			if ($this->nb_comments !== 0) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->sf_blog_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->author_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->title = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->stripped_title = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->extract = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->content = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->is_published = ($row[$startcol + 7] !== null) ? (boolean) $row[$startcol + 7] : null;
			$this->allow_comments = ($row[$startcol + 8] !== null) ? (boolean) $row[$startcol + 8] : null;
			$this->nb_comments = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->created_at = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->updated_at = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->published_at = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 13; // 13 = sfBlogPostPeer::NUM_COLUMNS - sfBlogPostPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating sfBlogPost object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->asfBlog !== null && $this->sf_blog_id !== $this->asfBlog->getId()) {
			$this->asfBlog = null;
		}
		if ($this->asfGuardUser !== null && $this->author_id !== $this->asfGuardUser->getId()) {
			$this->asfGuardUser = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(sfBlogPostPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = sfBlogPostPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->asfBlog = null;
			$this->asfGuardUser = null;
			$this->collsfBlogComments = null;
			$this->lastsfBlogCommentCriteria = null;

			$this->collsfBlogTags = null;
			$this->lastsfBlogTagCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BasesfBlogPost:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(sfBlogPostPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			sfBlogPostPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BasesfBlogPost:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BasesfBlogPost:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(sfBlogPostPeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

    if ($this->isModified() && !$this->isColumnModified(sfBlogPostPeer::UPDATED_AT))
    {
      $this->setUpdatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(sfBlogPostPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BasesfBlogPost:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			sfBlogPostPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->asfBlog !== null) {
				if ($this->asfBlog->isModified() || $this->asfBlog->isNew()) {
					$affectedRows += $this->asfBlog->save($con);
				}
				$this->setsfBlog($this->asfBlog);
			}

			if ($this->asfGuardUser !== null) {
				if ($this->asfGuardUser->isModified() || $this->asfGuardUser->isNew()) {
					$affectedRows += $this->asfGuardUser->save($con);
				}
				$this->setsfGuardUser($this->asfGuardUser);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = sfBlogPostPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = sfBlogPostPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += sfBlogPostPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collsfBlogComments !== null) {
				foreach ($this->collsfBlogComments as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collsfBlogTags !== null) {
				foreach ($this->collsfBlogTags as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->asfBlog !== null) {
				if (!$this->asfBlog->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->asfBlog->getValidationFailures());
				}
			}

			if ($this->asfGuardUser !== null) {
				if (!$this->asfGuardUser->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->asfGuardUser->getValidationFailures());
				}
			}


			if (($retval = sfBlogPostPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collsfBlogComments !== null) {
					foreach ($this->collsfBlogComments as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collsfBlogTags !== null) {
					foreach ($this->collsfBlogTags as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = sfBlogPostPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getSfBlogId();
				break;
			case 2:
				return $this->getAuthorId();
				break;
			case 3:
				return $this->getTitle();
				break;
			case 4:
				return $this->getStrippedTitle();
				break;
			case 5:
				return $this->getExtract();
				break;
			case 6:
				return $this->getContent();
				break;
			case 7:
				return $this->getIsPublished();
				break;
			case 8:
				return $this->getAllowComments();
				break;
			case 9:
				return $this->getNbComments();
				break;
			case 10:
				return $this->getCreatedAt();
				break;
			case 11:
				return $this->getUpdatedAt();
				break;
			case 12:
				return $this->getPublishedAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = sfBlogPostPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getSfBlogId(),
			$keys[2] => $this->getAuthorId(),
			$keys[3] => $this->getTitle(),
			$keys[4] => $this->getStrippedTitle(),
			$keys[5] => $this->getExtract(),
			$keys[6] => $this->getContent(),
			$keys[7] => $this->getIsPublished(),
			$keys[8] => $this->getAllowComments(),
			$keys[9] => $this->getNbComments(),
			$keys[10] => $this->getCreatedAt(),
			$keys[11] => $this->getUpdatedAt(),
			$keys[12] => $this->getPublishedAt(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = sfBlogPostPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setSfBlogId($value);
				break;
			case 2:
				$this->setAuthorId($value);
				break;
			case 3:
				$this->setTitle($value);
				break;
			case 4:
				$this->setStrippedTitle($value);
				break;
			case 5:
				$this->setExtract($value);
				break;
			case 6:
				$this->setContent($value);
				break;
			case 7:
				$this->setIsPublished($value);
				break;
			case 8:
				$this->setAllowComments($value);
				break;
			case 9:
				$this->setNbComments($value);
				break;
			case 10:
				$this->setCreatedAt($value);
				break;
			case 11:
				$this->setUpdatedAt($value);
				break;
			case 12:
				$this->setPublishedAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = sfBlogPostPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setSfBlogId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setAuthorId($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setTitle($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setStrippedTitle($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setExtract($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setContent($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setIsPublished($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setAllowComments($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setNbComments($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setCreatedAt($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setUpdatedAt($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setPublishedAt($arr[$keys[12]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(sfBlogPostPeer::DATABASE_NAME);

		if ($this->isColumnModified(sfBlogPostPeer::ID)) $criteria->add(sfBlogPostPeer::ID, $this->id);
		if ($this->isColumnModified(sfBlogPostPeer::SF_BLOG_ID)) $criteria->add(sfBlogPostPeer::SF_BLOG_ID, $this->sf_blog_id);
		if ($this->isColumnModified(sfBlogPostPeer::AUTHOR_ID)) $criteria->add(sfBlogPostPeer::AUTHOR_ID, $this->author_id);
		if ($this->isColumnModified(sfBlogPostPeer::TITLE)) $criteria->add(sfBlogPostPeer::TITLE, $this->title);
		if ($this->isColumnModified(sfBlogPostPeer::STRIPPED_TITLE)) $criteria->add(sfBlogPostPeer::STRIPPED_TITLE, $this->stripped_title);
		if ($this->isColumnModified(sfBlogPostPeer::EXTRACT)) $criteria->add(sfBlogPostPeer::EXTRACT, $this->extract);
		if ($this->isColumnModified(sfBlogPostPeer::CONTENT)) $criteria->add(sfBlogPostPeer::CONTENT, $this->content);
		if ($this->isColumnModified(sfBlogPostPeer::IS_PUBLISHED)) $criteria->add(sfBlogPostPeer::IS_PUBLISHED, $this->is_published);
		if ($this->isColumnModified(sfBlogPostPeer::ALLOW_COMMENTS)) $criteria->add(sfBlogPostPeer::ALLOW_COMMENTS, $this->allow_comments);
		if ($this->isColumnModified(sfBlogPostPeer::NB_COMMENTS)) $criteria->add(sfBlogPostPeer::NB_COMMENTS, $this->nb_comments);
		if ($this->isColumnModified(sfBlogPostPeer::CREATED_AT)) $criteria->add(sfBlogPostPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(sfBlogPostPeer::UPDATED_AT)) $criteria->add(sfBlogPostPeer::UPDATED_AT, $this->updated_at);
		if ($this->isColumnModified(sfBlogPostPeer::PUBLISHED_AT)) $criteria->add(sfBlogPostPeer::PUBLISHED_AT, $this->published_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(sfBlogPostPeer::DATABASE_NAME);

		$criteria->add(sfBlogPostPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of sfBlogPost (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setSfBlogId($this->sf_blog_id);

		$copyObj->setAuthorId($this->author_id);

		$copyObj->setTitle($this->title);

		$copyObj->setStrippedTitle($this->stripped_title);

		$copyObj->setExtract($this->extract);

		$copyObj->setContent($this->content);

		$copyObj->setIsPublished($this->is_published);

		$copyObj->setAllowComments($this->allow_comments);

		$copyObj->setNbComments($this->nb_comments);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);

		$copyObj->setPublishedAt($this->published_at);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getsfBlogComments() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addsfBlogComment($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getsfBlogTags() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addsfBlogTag($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     sfBlogPost Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     sfBlogPostPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new sfBlogPostPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a sfBlog object.
	 *
	 * @param      sfBlog $v
	 * @return     sfBlogPost The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setsfBlog(sfBlog $v = null)
	{
		if ($v === null) {
			$this->setSfBlogId(NULL);
		} else {
			$this->setSfBlogId($v->getId());
		}

		$this->asfBlog = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the sfBlog object, it will not be re-added.
		if ($v !== null) {
			$v->addsfBlogPost($this);
		}

		return $this;
	}


	/**
	 * Get the associated sfBlog object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     sfBlog The associated sfBlog object.
	 * @throws     PropelException
	 */
	public function getsfBlog(PropelPDO $con = null)
	{
		if ($this->asfBlog === null && ($this->sf_blog_id !== null)) {
			$c = new Criteria(sfBlogPeer::DATABASE_NAME);
			$c->add(sfBlogPeer::ID, $this->sf_blog_id);
			$this->asfBlog = sfBlogPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->asfBlog->addsfBlogPosts($this);
			 */
		}
		return $this->asfBlog;
	}

	/**
	 * Declares an association between this object and a sfGuardUser object.
	 *
	 * @param      sfGuardUser $v
	 * @return     sfBlogPost The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setsfGuardUser(sfGuardUser $v = null)
	{
		if ($v === null) {
			$this->setAuthorId(NULL);
		} else {
			$this->setAuthorId($v->getId());
		}

		$this->asfGuardUser = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the sfGuardUser object, it will not be re-added.
		if ($v !== null) {
			$v->addsfBlogPost($this);
		}

		return $this;
	}


	/**
	 * Get the associated sfGuardUser object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     sfGuardUser The associated sfGuardUser object.
	 * @throws     PropelException
	 */
	public function getsfGuardUser(PropelPDO $con = null)
	{
		if ($this->asfGuardUser === null && ($this->author_id !== null)) {
			$c = new Criteria(sfGuardUserPeer::DATABASE_NAME);
			$c->add(sfGuardUserPeer::ID, $this->author_id);
			$this->asfGuardUser = sfGuardUserPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->asfGuardUser->addsfBlogPosts($this);
			 */
		}
		return $this->asfGuardUser;
	}

	/**
	 * Clears out the collsfBlogComments collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addsfBlogComments()
	 */
	public function clearsfBlogComments()
	{
		$this->collsfBlogComments = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collsfBlogComments collection (array).
	 *
	 * By default this just sets the collsfBlogComments collection to an empty array (like clearcollsfBlogComments());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initsfBlogComments()
	{
		$this->collsfBlogComments = array();
	}

	/**
	 * Gets an array of sfBlogComment objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this sfBlogPost has previously been saved, it will retrieve
	 * related sfBlogComments from storage. If this sfBlogPost is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array sfBlogComment[]
	 * @throws     PropelException
	 */
	public function getsfBlogComments($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(sfBlogPostPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collsfBlogComments === null) {
			if ($this->isNew()) {
			   $this->collsfBlogComments = array();
			} else {

				$criteria->add(sfBlogCommentPeer::SF_BLOG_POST_ID, $this->id);

				sfBlogCommentPeer::addSelectColumns($criteria);
				$this->collsfBlogComments = sfBlogCommentPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(sfBlogCommentPeer::SF_BLOG_POST_ID, $this->id);

				sfBlogCommentPeer::addSelectColumns($criteria);
				if (!isset($this->lastsfBlogCommentCriteria) || !$this->lastsfBlogCommentCriteria->equals($criteria)) {
					$this->collsfBlogComments = sfBlogCommentPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastsfBlogCommentCriteria = $criteria;
		return $this->collsfBlogComments;
	}

	/**
	 * Returns the number of related sfBlogComment objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related sfBlogComment objects.
	 * @throws     PropelException
	 */
	public function countsfBlogComments(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(sfBlogPostPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collsfBlogComments === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(sfBlogCommentPeer::SF_BLOG_POST_ID, $this->id);

				$count = sfBlogCommentPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(sfBlogCommentPeer::SF_BLOG_POST_ID, $this->id);

				if (!isset($this->lastsfBlogCommentCriteria) || !$this->lastsfBlogCommentCriteria->equals($criteria)) {
					$count = sfBlogCommentPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collsfBlogComments);
				}
			} else {
				$count = count($this->collsfBlogComments);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a sfBlogComment object to this object
	 * through the sfBlogComment foreign key attribute.
	 *
	 * @param      sfBlogComment $l sfBlogComment
	 * @return     void
	 * @throws     PropelException
	 */
	public function addsfBlogComment(sfBlogComment $l)
	{
		if ($this->collsfBlogComments === null) {
			$this->initsfBlogComments();
		}
		if (!in_array($l, $this->collsfBlogComments, true)) { // only add it if the **same** object is not already associated
			array_push($this->collsfBlogComments, $l);
			$l->setsfBlogPost($this);
		}
	}

	/**
	 * Clears out the collsfBlogTags collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addsfBlogTags()
	 */
	public function clearsfBlogTags()
	{
		$this->collsfBlogTags = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collsfBlogTags collection (array).
	 *
	 * By default this just sets the collsfBlogTags collection to an empty array (like clearcollsfBlogTags());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initsfBlogTags()
	{
		$this->collsfBlogTags = array();
	}

	/**
	 * Gets an array of sfBlogTag objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this sfBlogPost has previously been saved, it will retrieve
	 * related sfBlogTags from storage. If this sfBlogPost is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array sfBlogTag[]
	 * @throws     PropelException
	 */
	public function getsfBlogTags($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(sfBlogPostPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collsfBlogTags === null) {
			if ($this->isNew()) {
			   $this->collsfBlogTags = array();
			} else {

				$criteria->add(sfBlogTagPeer::SF_BLOG_POST_ID, $this->id);

				sfBlogTagPeer::addSelectColumns($criteria);
				$this->collsfBlogTags = sfBlogTagPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(sfBlogTagPeer::SF_BLOG_POST_ID, $this->id);

				sfBlogTagPeer::addSelectColumns($criteria);
				if (!isset($this->lastsfBlogTagCriteria) || !$this->lastsfBlogTagCriteria->equals($criteria)) {
					$this->collsfBlogTags = sfBlogTagPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastsfBlogTagCriteria = $criteria;
		return $this->collsfBlogTags;
	}

	/**
	 * Returns the number of related sfBlogTag objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related sfBlogTag objects.
	 * @throws     PropelException
	 */
	public function countsfBlogTags(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(sfBlogPostPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collsfBlogTags === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(sfBlogTagPeer::SF_BLOG_POST_ID, $this->id);

				$count = sfBlogTagPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(sfBlogTagPeer::SF_BLOG_POST_ID, $this->id);

				if (!isset($this->lastsfBlogTagCriteria) || !$this->lastsfBlogTagCriteria->equals($criteria)) {
					$count = sfBlogTagPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collsfBlogTags);
				}
			} else {
				$count = count($this->collsfBlogTags);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a sfBlogTag object to this object
	 * through the sfBlogTag foreign key attribute.
	 *
	 * @param      sfBlogTag $l sfBlogTag
	 * @return     void
	 * @throws     PropelException
	 */
	public function addsfBlogTag(sfBlogTag $l)
	{
		if ($this->collsfBlogTags === null) {
			$this->initsfBlogTags();
		}
		if (!in_array($l, $this->collsfBlogTags, true)) { // only add it if the **same** object is not already associated
			array_push($this->collsfBlogTags, $l);
			$l->setsfBlogPost($this);
		}
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collsfBlogComments) {
				foreach ((array) $this->collsfBlogComments as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collsfBlogTags) {
				foreach ((array) $this->collsfBlogTags as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collsfBlogComments = null;
		$this->collsfBlogTags = null;
			$this->asfBlog = null;
			$this->asfGuardUser = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BasesfBlogPost:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BasesfBlogPost::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BasesfBlogPost
