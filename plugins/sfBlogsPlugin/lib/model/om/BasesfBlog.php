<?php

/**
 * Base class that represents a row from the 'sf_blog' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Wed Apr 22 19:56:25 2009
 *
 * @package    plugins.sfBlogsPlugin.lib.model.om
 */
abstract class BasesfBlog extends BaseObject  implements Persistent {


  const PEER = 'sfBlogPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        sfBlogPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the title field.
	 * @var        string
	 */
	protected $title;

	/**
	 * The value for the tagline field.
	 * @var        string
	 */
	protected $tagline;

	/**
	 * The value for the copyright field.
	 * @var        string
	 */
	protected $copyright;

	/**
	 * The value for the stripped_title field.
	 * @var        string
	 */
	protected $stripped_title;

	/**
	 * The value for the is_published field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_published;

	/**
	 * The value for the is_finished field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_finished;

	/**
	 * The value for the display_extract field.
	 * Note: this column has a database default value of: true
	 * @var        boolean
	 */
	protected $display_extract;

	/**
	 * The value for the comment_policy field.
	 * Note: this column has a database default value of: 2
	 * @var        int
	 */
	protected $comment_policy;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * @var        array sfBlogPost[] Collection to store aggregation of sfBlogPost objects.
	 */
	protected $collsfBlogPosts;

	/**
	 * @var        Criteria The criteria used to select the current contents of collsfBlogPosts.
	 */
	private $lastsfBlogPostCriteria = null;

	/**
	 * @var        array sfBlogUser[] Collection to store aggregation of sfBlogUser objects.
	 */
	protected $collsfBlogUsers;

	/**
	 * @var        Criteria The criteria used to select the current contents of collsfBlogUsers.
	 */
	private $lastsfBlogUserCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BasesfBlog object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->is_published = false;
		$this->is_finished = false;
		$this->display_extract = true;
		$this->comment_policy = 2;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [title] column value.
	 * 
	 * @return     string
	 */
	public function getTitle()
	{
		return $this->title;
	}

	/**
	 * Get the [tagline] column value.
	 * 
	 * @return     string
	 */
	public function getTagline()
	{
		return $this->tagline;
	}

	/**
	 * Get the [copyright] column value.
	 * 
	 * @return     string
	 */
	public function getCopyright()
	{
		return $this->copyright;
	}

	/**
	 * Get the [stripped_title] column value.
	 * 
	 * @return     string
	 */
	public function getStrippedTitle()
	{
		return $this->stripped_title;
	}

	/**
	 * Get the [is_published] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsPublished()
	{
		return $this->is_published;
	}

	/**
	 * Get the [is_finished] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsFinished()
	{
		return $this->is_finished;
	}

	/**
	 * Get the [display_extract] column value.
	 * 
	 * @return     boolean
	 */
	public function getDisplayExtract()
	{
		return $this->display_extract;
	}

	/**
	 * Get the [comment_policy] column value.
	 * 
	 * @return     int
	 */
	public function getCommentPolicy()
	{
		return $this->comment_policy;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}


		if ($this->updated_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->updated_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     sfBlog The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = sfBlogPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [title] column.
	 * 
	 * @param      string $v new value
	 * @return     sfBlog The current object (for fluent API support)
	 */
	public function setTitle($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->title !== $v) {
			$this->title = $v;
			$this->modifiedColumns[] = sfBlogPeer::TITLE;
		}

		return $this;
	} // setTitle()

	/**
	 * Set the value of [tagline] column.
	 * 
	 * @param      string $v new value
	 * @return     sfBlog The current object (for fluent API support)
	 */
	public function setTagline($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tagline !== $v) {
			$this->tagline = $v;
			$this->modifiedColumns[] = sfBlogPeer::TAGLINE;
		}

		return $this;
	} // setTagline()

	/**
	 * Set the value of [copyright] column.
	 * 
	 * @param      string $v new value
	 * @return     sfBlog The current object (for fluent API support)
	 */
	public function setCopyright($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->copyright !== $v) {
			$this->copyright = $v;
			$this->modifiedColumns[] = sfBlogPeer::COPYRIGHT;
		}

		return $this;
	} // setCopyright()

	/**
	 * Set the value of [stripped_title] column.
	 * 
	 * @param      string $v new value
	 * @return     sfBlog The current object (for fluent API support)
	 */
	public function setStrippedTitle($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->stripped_title !== $v) {
			$this->stripped_title = $v;
			$this->modifiedColumns[] = sfBlogPeer::STRIPPED_TITLE;
		}

		return $this;
	} // setStrippedTitle()

	/**
	 * Set the value of [is_published] column.
	 * 
	 * @param      boolean $v new value
	 * @return     sfBlog The current object (for fluent API support)
	 */
	public function setIsPublished($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_published !== $v || $v === false) {
			$this->is_published = $v;
			$this->modifiedColumns[] = sfBlogPeer::IS_PUBLISHED;
		}

		return $this;
	} // setIsPublished()

	/**
	 * Set the value of [is_finished] column.
	 * 
	 * @param      boolean $v new value
	 * @return     sfBlog The current object (for fluent API support)
	 */
	public function setIsFinished($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_finished !== $v || $v === false) {
			$this->is_finished = $v;
			$this->modifiedColumns[] = sfBlogPeer::IS_FINISHED;
		}

		return $this;
	} // setIsFinished()

	/**
	 * Set the value of [display_extract] column.
	 * 
	 * @param      boolean $v new value
	 * @return     sfBlog The current object (for fluent API support)
	 */
	public function setDisplayExtract($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->display_extract !== $v || $v === true) {
			$this->display_extract = $v;
			$this->modifiedColumns[] = sfBlogPeer::DISPLAY_EXTRACT;
		}

		return $this;
	} // setDisplayExtract()

	/**
	 * Set the value of [comment_policy] column.
	 * 
	 * @param      int $v new value
	 * @return     sfBlog The current object (for fluent API support)
	 */
	public function setCommentPolicy($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->comment_policy !== $v || $v === 2) {
			$this->comment_policy = $v;
			$this->modifiedColumns[] = sfBlogPeer::COMMENT_POLICY;
		}

		return $this;
	} // setCommentPolicy()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     sfBlog The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = sfBlogPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     sfBlog The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = sfBlogPeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(sfBlogPeer::IS_PUBLISHED,sfBlogPeer::IS_FINISHED,sfBlogPeer::DISPLAY_EXTRACT,sfBlogPeer::COMMENT_POLICY))) {
				return false;
			}

			if ($this->is_published !== false) {
				return false;
			}

			if ($this->is_finished !== false) {
				return false;
			}

			if ($this->display_extract !== true) {
				return false;
			}

			if ($this->comment_policy !== 2) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->title = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->tagline = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->copyright = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->stripped_title = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->is_published = ($row[$startcol + 5] !== null) ? (boolean) $row[$startcol + 5] : null;
			$this->is_finished = ($row[$startcol + 6] !== null) ? (boolean) $row[$startcol + 6] : null;
			$this->display_extract = ($row[$startcol + 7] !== null) ? (boolean) $row[$startcol + 7] : null;
			$this->comment_policy = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
			$this->created_at = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->updated_at = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 11; // 11 = sfBlogPeer::NUM_COLUMNS - sfBlogPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating sfBlog object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(sfBlogPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = sfBlogPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collsfBlogPosts = null;
			$this->lastsfBlogPostCriteria = null;

			$this->collsfBlogUsers = null;
			$this->lastsfBlogUserCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BasesfBlog:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(sfBlogPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			sfBlogPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BasesfBlog:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BasesfBlog:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(sfBlogPeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

    if ($this->isModified() && !$this->isColumnModified(sfBlogPeer::UPDATED_AT))
    {
      $this->setUpdatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(sfBlogPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BasesfBlog:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			sfBlogPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			if ($this->isNew() ) {
				$this->modifiedColumns[] = sfBlogPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = sfBlogPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += sfBlogPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collsfBlogPosts !== null) {
				foreach ($this->collsfBlogPosts as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collsfBlogUsers !== null) {
				foreach ($this->collsfBlogUsers as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = sfBlogPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collsfBlogPosts !== null) {
					foreach ($this->collsfBlogPosts as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collsfBlogUsers !== null) {
					foreach ($this->collsfBlogUsers as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = sfBlogPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getTitle();
				break;
			case 2:
				return $this->getTagline();
				break;
			case 3:
				return $this->getCopyright();
				break;
			case 4:
				return $this->getStrippedTitle();
				break;
			case 5:
				return $this->getIsPublished();
				break;
			case 6:
				return $this->getIsFinished();
				break;
			case 7:
				return $this->getDisplayExtract();
				break;
			case 8:
				return $this->getCommentPolicy();
				break;
			case 9:
				return $this->getCreatedAt();
				break;
			case 10:
				return $this->getUpdatedAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = sfBlogPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getTitle(),
			$keys[2] => $this->getTagline(),
			$keys[3] => $this->getCopyright(),
			$keys[4] => $this->getStrippedTitle(),
			$keys[5] => $this->getIsPublished(),
			$keys[6] => $this->getIsFinished(),
			$keys[7] => $this->getDisplayExtract(),
			$keys[8] => $this->getCommentPolicy(),
			$keys[9] => $this->getCreatedAt(),
			$keys[10] => $this->getUpdatedAt(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = sfBlogPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setTitle($value);
				break;
			case 2:
				$this->setTagline($value);
				break;
			case 3:
				$this->setCopyright($value);
				break;
			case 4:
				$this->setStrippedTitle($value);
				break;
			case 5:
				$this->setIsPublished($value);
				break;
			case 6:
				$this->setIsFinished($value);
				break;
			case 7:
				$this->setDisplayExtract($value);
				break;
			case 8:
				$this->setCommentPolicy($value);
				break;
			case 9:
				$this->setCreatedAt($value);
				break;
			case 10:
				$this->setUpdatedAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = sfBlogPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setTitle($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setTagline($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setCopyright($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setStrippedTitle($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setIsPublished($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setIsFinished($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setDisplayExtract($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setCommentPolicy($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setCreatedAt($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setUpdatedAt($arr[$keys[10]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(sfBlogPeer::DATABASE_NAME);

		if ($this->isColumnModified(sfBlogPeer::ID)) $criteria->add(sfBlogPeer::ID, $this->id);
		if ($this->isColumnModified(sfBlogPeer::TITLE)) $criteria->add(sfBlogPeer::TITLE, $this->title);
		if ($this->isColumnModified(sfBlogPeer::TAGLINE)) $criteria->add(sfBlogPeer::TAGLINE, $this->tagline);
		if ($this->isColumnModified(sfBlogPeer::COPYRIGHT)) $criteria->add(sfBlogPeer::COPYRIGHT, $this->copyright);
		if ($this->isColumnModified(sfBlogPeer::STRIPPED_TITLE)) $criteria->add(sfBlogPeer::STRIPPED_TITLE, $this->stripped_title);
		if ($this->isColumnModified(sfBlogPeer::IS_PUBLISHED)) $criteria->add(sfBlogPeer::IS_PUBLISHED, $this->is_published);
		if ($this->isColumnModified(sfBlogPeer::IS_FINISHED)) $criteria->add(sfBlogPeer::IS_FINISHED, $this->is_finished);
		if ($this->isColumnModified(sfBlogPeer::DISPLAY_EXTRACT)) $criteria->add(sfBlogPeer::DISPLAY_EXTRACT, $this->display_extract);
		if ($this->isColumnModified(sfBlogPeer::COMMENT_POLICY)) $criteria->add(sfBlogPeer::COMMENT_POLICY, $this->comment_policy);
		if ($this->isColumnModified(sfBlogPeer::CREATED_AT)) $criteria->add(sfBlogPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(sfBlogPeer::UPDATED_AT)) $criteria->add(sfBlogPeer::UPDATED_AT, $this->updated_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(sfBlogPeer::DATABASE_NAME);

		$criteria->add(sfBlogPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of sfBlog (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setTitle($this->title);

		$copyObj->setTagline($this->tagline);

		$copyObj->setCopyright($this->copyright);

		$copyObj->setStrippedTitle($this->stripped_title);

		$copyObj->setIsPublished($this->is_published);

		$copyObj->setIsFinished($this->is_finished);

		$copyObj->setDisplayExtract($this->display_extract);

		$copyObj->setCommentPolicy($this->comment_policy);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getsfBlogPosts() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addsfBlogPost($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getsfBlogUsers() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addsfBlogUser($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     sfBlog Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     sfBlogPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new sfBlogPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collsfBlogPosts collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addsfBlogPosts()
	 */
	public function clearsfBlogPosts()
	{
		$this->collsfBlogPosts = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collsfBlogPosts collection (array).
	 *
	 * By default this just sets the collsfBlogPosts collection to an empty array (like clearcollsfBlogPosts());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initsfBlogPosts()
	{
		$this->collsfBlogPosts = array();
	}

	/**
	 * Gets an array of sfBlogPost objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this sfBlog has previously been saved, it will retrieve
	 * related sfBlogPosts from storage. If this sfBlog is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array sfBlogPost[]
	 * @throws     PropelException
	 */
	public function getsfBlogPosts($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(sfBlogPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collsfBlogPosts === null) {
			if ($this->isNew()) {
			   $this->collsfBlogPosts = array();
			} else {

				$criteria->add(sfBlogPostPeer::SF_BLOG_ID, $this->id);

				sfBlogPostPeer::addSelectColumns($criteria);
				$this->collsfBlogPosts = sfBlogPostPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(sfBlogPostPeer::SF_BLOG_ID, $this->id);

				sfBlogPostPeer::addSelectColumns($criteria);
				if (!isset($this->lastsfBlogPostCriteria) || !$this->lastsfBlogPostCriteria->equals($criteria)) {
					$this->collsfBlogPosts = sfBlogPostPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastsfBlogPostCriteria = $criteria;
		return $this->collsfBlogPosts;
	}

	/**
	 * Returns the number of related sfBlogPost objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related sfBlogPost objects.
	 * @throws     PropelException
	 */
	public function countsfBlogPosts(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(sfBlogPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collsfBlogPosts === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(sfBlogPostPeer::SF_BLOG_ID, $this->id);

				$count = sfBlogPostPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(sfBlogPostPeer::SF_BLOG_ID, $this->id);

				if (!isset($this->lastsfBlogPostCriteria) || !$this->lastsfBlogPostCriteria->equals($criteria)) {
					$count = sfBlogPostPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collsfBlogPosts);
				}
			} else {
				$count = count($this->collsfBlogPosts);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a sfBlogPost object to this object
	 * through the sfBlogPost foreign key attribute.
	 *
	 * @param      sfBlogPost $l sfBlogPost
	 * @return     void
	 * @throws     PropelException
	 */
	public function addsfBlogPost(sfBlogPost $l)
	{
		if ($this->collsfBlogPosts === null) {
			$this->initsfBlogPosts();
		}
		if (!in_array($l, $this->collsfBlogPosts, true)) { // only add it if the **same** object is not already associated
			array_push($this->collsfBlogPosts, $l);
			$l->setsfBlog($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this sfBlog is new, it will return
	 * an empty collection; or if this sfBlog has previously
	 * been saved, it will retrieve related sfBlogPosts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in sfBlog.
	 */
	public function getsfBlogPostsJoinsfGuardUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(sfBlogPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collsfBlogPosts === null) {
			if ($this->isNew()) {
				$this->collsfBlogPosts = array();
			} else {

				$criteria->add(sfBlogPostPeer::SF_BLOG_ID, $this->id);

				$this->collsfBlogPosts = sfBlogPostPeer::doSelectJoinsfGuardUser($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(sfBlogPostPeer::SF_BLOG_ID, $this->id);

			if (!isset($this->lastsfBlogPostCriteria) || !$this->lastsfBlogPostCriteria->equals($criteria)) {
				$this->collsfBlogPosts = sfBlogPostPeer::doSelectJoinsfGuardUser($criteria, $con, $join_behavior);
			}
		}
		$this->lastsfBlogPostCriteria = $criteria;

		return $this->collsfBlogPosts;
	}

	/**
	 * Clears out the collsfBlogUsers collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addsfBlogUsers()
	 */
	public function clearsfBlogUsers()
	{
		$this->collsfBlogUsers = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collsfBlogUsers collection (array).
	 *
	 * By default this just sets the collsfBlogUsers collection to an empty array (like clearcollsfBlogUsers());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initsfBlogUsers()
	{
		$this->collsfBlogUsers = array();
	}

	/**
	 * Gets an array of sfBlogUser objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this sfBlog has previously been saved, it will retrieve
	 * related sfBlogUsers from storage. If this sfBlog is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array sfBlogUser[]
	 * @throws     PropelException
	 */
	public function getsfBlogUsers($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(sfBlogPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collsfBlogUsers === null) {
			if ($this->isNew()) {
			   $this->collsfBlogUsers = array();
			} else {

				$criteria->add(sfBlogUserPeer::SF_BLOG_ID, $this->id);

				sfBlogUserPeer::addSelectColumns($criteria);
				$this->collsfBlogUsers = sfBlogUserPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(sfBlogUserPeer::SF_BLOG_ID, $this->id);

				sfBlogUserPeer::addSelectColumns($criteria);
				if (!isset($this->lastsfBlogUserCriteria) || !$this->lastsfBlogUserCriteria->equals($criteria)) {
					$this->collsfBlogUsers = sfBlogUserPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastsfBlogUserCriteria = $criteria;
		return $this->collsfBlogUsers;
	}

	/**
	 * Returns the number of related sfBlogUser objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related sfBlogUser objects.
	 * @throws     PropelException
	 */
	public function countsfBlogUsers(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(sfBlogPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collsfBlogUsers === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(sfBlogUserPeer::SF_BLOG_ID, $this->id);

				$count = sfBlogUserPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(sfBlogUserPeer::SF_BLOG_ID, $this->id);

				if (!isset($this->lastsfBlogUserCriteria) || !$this->lastsfBlogUserCriteria->equals($criteria)) {
					$count = sfBlogUserPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collsfBlogUsers);
				}
			} else {
				$count = count($this->collsfBlogUsers);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a sfBlogUser object to this object
	 * through the sfBlogUser foreign key attribute.
	 *
	 * @param      sfBlogUser $l sfBlogUser
	 * @return     void
	 * @throws     PropelException
	 */
	public function addsfBlogUser(sfBlogUser $l)
	{
		if ($this->collsfBlogUsers === null) {
			$this->initsfBlogUsers();
		}
		if (!in_array($l, $this->collsfBlogUsers, true)) { // only add it if the **same** object is not already associated
			array_push($this->collsfBlogUsers, $l);
			$l->setsfBlog($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this sfBlog is new, it will return
	 * an empty collection; or if this sfBlog has previously
	 * been saved, it will retrieve related sfBlogUsers from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in sfBlog.
	 */
	public function getsfBlogUsersJoinsfGuardUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(sfBlogPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collsfBlogUsers === null) {
			if ($this->isNew()) {
				$this->collsfBlogUsers = array();
			} else {

				$criteria->add(sfBlogUserPeer::SF_BLOG_ID, $this->id);

				$this->collsfBlogUsers = sfBlogUserPeer::doSelectJoinsfGuardUser($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(sfBlogUserPeer::SF_BLOG_ID, $this->id);

			if (!isset($this->lastsfBlogUserCriteria) || !$this->lastsfBlogUserCriteria->equals($criteria)) {
				$this->collsfBlogUsers = sfBlogUserPeer::doSelectJoinsfGuardUser($criteria, $con, $join_behavior);
			}
		}
		$this->lastsfBlogUserCriteria = $criteria;

		return $this->collsfBlogUsers;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collsfBlogPosts) {
				foreach ((array) $this->collsfBlogPosts as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collsfBlogUsers) {
				foreach ((array) $this->collsfBlogUsers as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collsfBlogPosts = null;
		$this->collsfBlogUsers = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BasesfBlog:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BasesfBlog::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BasesfBlog
