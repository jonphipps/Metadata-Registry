machine:
  php:
    version: 7.1
  node:
    version: 7.6
  services:
    - redis
    - memcached
  hosts:
    registry.dev: 127.0.0.1

checkout:
  post:
    - mv /.env.circle .env

dependencies:
  override:
    - yarn
  post:
    - npm run production
