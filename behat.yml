default:
    extensions:
        Laracasts\Behat:
            # env_path: .env.behat
        Behat\MinkExtension:
            base_url: http://registry.dev
            default_session: laravel
            laravel: ~
            goutte: ~
