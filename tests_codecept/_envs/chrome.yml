# `chrome` environment config goes here
modules:
    enabled:
        - WebDriver
        - \Helper\Acceptance
    config:
        WebDriver:
            url: 'http://registry.test/'
            browser: 'chrome'
